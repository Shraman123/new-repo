# Verification checklist

One row per number/rule drawn from `data/schemes/`, `data/params/`, and
`data/directory/`. Generated (by hand for now) so a human can check every
figure against the cited source PDF and page, then flip `verified: true` in
the YAML.

There is nothing to verify yet — no scheme cards, parameters or directory
entries exist. This table is regenerated once official PDFs are in
`data/sources/` and draft cards exist (Phase 1's manual step in
`PROMPTS.md`).

| Card/file id | Field | Value | Source file | Page | Verified? |
|---|---|---|---|---|---|
| _(none yet)_ | | | | | |
