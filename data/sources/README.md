# Sources

Official PDFs only, downloaded by hand from central/state government or department
sites — never edited after download. List every file added here:

| File | Title | URL | Retrieved on |
|---|---|---|---|
| _(none yet)_ | | | |

Nothing has been added yet. Scheme cards in `data/schemes/` and calculator
parameters in `data/params/` cannot be drafted from real facts until PDFs are
here — see `PROMPTS.md` Phase 1's "before you start" step.
