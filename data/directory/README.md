# Office directory

One YAML file per district (e.g. `jhargram.yaml`), matching the shape in
`docs/SPEC.md` §5. Contacts come only from official district/department
websites. Empty until district notes are supplied (see `PROMPTS.md`
"before you start").
